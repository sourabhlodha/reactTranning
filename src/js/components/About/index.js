import React from 'react';

const About = () => (
  <div>
    <h1>About WDS d</h1>
    <h2>Quick Start</h2>
    <h3>About Page new about lnks</h3>
    <p>The easiest way to get started with a React web project is with a tool called Create React App, a Facebook project with a ton of community help.First install create-react-app if you don’t already have it, and then make a new project with it.</p>
  </div>
);

export default About;